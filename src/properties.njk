---
layout: layouts/base.njk
title: Properties
seo:
  title: Luxury Properties in Latvia | Vandore Heritage
  description: Explore heritage homes, penthouses, and executive rentals curated by Vandore Heritage.
_enabled_editors:
  - visual
  - data
hero:
  eyebrow: Properties
  headline: Curated residences ready for discerning households
  subheadline: Each property below is represented exclusively by Vandore Heritage with private viewings available on request.
  cta_label: Share your search brief
  cta_url: /contact/
---
<section class="hero">
  <div class="container">
    <p data-cms-bind="#hero.eyebrow">{{ hero.eyebrow }}</p>
    <h1 data-cms-bind="#hero.headline">{{ hero.headline }}</h1>
    <p data-cms-bind="#hero.subheadline">{{ hero.subheadline }}</p>
    <a class="cta" href="{{ hero.cta_url }}"><span data-cms-bind="#hero.cta_label">{{ hero.cta_label }}</span></a>
  </div>
</section>

<section class="section">
  <div class="container grid two">
    {% for property in properties.list %}
    <article class="card" data-cms-collection="properties" data-cms-item="{{ property.slug }}">
      <img src="{{ property.gallery[0] }}" alt="{{ property.title }}" data-cms-bind="gallery[0]">
      <h2 data-cms-bind="title"><a href="/properties/{{ property.slug }}/">{{ property.title }}</a></h2>
      <p class="meta">
        <span data-cms-bind="city">{{ property.city }}</span>,
        <span data-cms-bind="area">{{ property.area }}</span>
      </p>
      <p data-cms-bind="hero.subheadline">{{ property.hero.subheadline }}</p>
      <p class="meta">
        <strong data-cms-bind="price">{{ property.price }}</strong>
        <span data-cms-bind="currency">{{ property.currency }}</span>
      </p>
      <a class="cta" href="/properties/{{ property.slug }}/"><span data-cms-bind="hero.cta_label">{{ property.hero.cta_label }}</span></a>
    </article>
    {% endfor %}
  </div>
</section>

